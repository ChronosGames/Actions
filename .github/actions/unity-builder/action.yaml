# see: https://github.com/game-ci/unity-builder
name: Unity Builder
description: "Build Unity projects for different platforms."
inputs:
  targetPlatform:
    required: true
    default: ""
    description: "Platform that the build should target."
  unityVersion:
    required: false
    default: "auto"
    description: 'Version of unity to use for building the project. Use "auto" to get from your ProjectSettings/ProjectVersion.txt'
  projectPath:
    required: true
    default: ""
    description: "Relative path to the project to be built."
  buildMethod:
    required: true
    default: ""
    description: "Path to a Namespace.Class.StaticMethod to run to perform the build."
  customParameters:
    required: false
    default: ""
    description: "Custom parameters to configure the build."
  versioning:
    required: false
    default: "None"
    description: "The versioning scheme to use when building the project"
  unityLicense:
    required: false
    default: ""
    description: "Unity .ulf license file contents. Pass secrets.UNITY_LICENSE, or set UNITY_LICENSE in the calling job environment."
  unitySerial:
    required: false
    default: ""
    description: "Unity Pro/Plus serial number. Pass secrets.UNITY_SERIAL, or set UNITY_SERIAL in the calling job environment."
runs:
  using: "composite"
  steps:
    - name: Validate Unity license credentials
      if: ${{ inputs.unityLicense == '' && env.UNITY_LICENSE == '' && inputs.unitySerial == '' && env.UNITY_SERIAL == '' }}
      shell: bash
      run: |
        echo "::error title=Unity license credential missing::Set unityLicense (or UNITY_LICENSE) for a Personal license, or unitySerial (or UNITY_SERIAL) for a Pro/Plus license. UNITY_EMAIL and UNITY_PASSWORD alone cannot activate Unity."
        echo "See https://game.ci/docs/github/activation for activation instructions."
        exit 1

    # see: https://github.com/game-ci/unity-builder
    # see: https://game.ci/docs/github/builder/
    - name: Build Unity
      uses: game-ci/unity-builder@v4
      env:
        FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
        UNITY_LICENSE: ${{ inputs.unityLicense || env.UNITY_LICENSE }}
        UNITY_SERIAL: ${{ inputs.unitySerial || env.UNITY_SERIAL }}
      with:
        targetPlatform: ${{ inputs.targetPlatform }}
        unityVersion: ${{ inputs.unityVersion }}
        projectPath: ${{ inputs.projectPath }}
        buildMethod: ${{ inputs.buildMethod }}
        customParameters: ${{ inputs.customParameters }}
        versioning: ${{ inputs.versioning }}
